/**
 * 测试数据生成脚本
 * 使用方法：用Node.js运行此脚本生成测试Excel
 * node generate-test-data.js
 *
 * 或者直接在浏览器中点击"加载测试数据"按钮
 */

// 如果在Node.js环境中运行
if (typeof require !== 'undefined') {
    const XLSX = require('xlsx');

    const testData = [
        // 标题行
        [
            '销售员姓名', '项目名称', '当前阶段', '项目金额(万元)', '进入当前阶段日期',
            '本月初阶段', '本月末阶段',
            '1.1 MAN分析与项目立项', '1.2 决策链地图绘制', '1.3 竞品深度分析', '1.4 技术标准影响计划',
            '2.1 需求深度调研', '2.2 客户化方案设计', '2.3 方案讲解与反馈收集',
            '3.1 决策链地图深化', '3.2 样品准备与现场展示', '3.3 竞品方案对比展示',
            '4.1 最终方案优化与提交', '4.2 入围确认与竞品信息跟踪', '4.3 投标策略制定与投标文件准备',
            '5.1 评分模拟与风险应对',
            '6.1 商务谈判与让步策略',
            '7.1 合同签订与项目交接',
            '进攻计划得分'
        ],
        // 张三数据
        [
            '张三', 'XX集团服装采购项目', '深入', 500, '2024-11-25',
            '接触', '深入',
            '100%', '100%', '100%', '100%',  // 1.1-1.4
            '100%', '100%', '100%',          // 2.1-2.3
            '100%', '50%', '100%',           // 3.1-3.3 (3.2是50%)
            '50%', '0%', '0%',               // 4.1-4.3 (4.1是50%)
            '0%',                            // 5.1
            '0%',                            // 6.1
            '0%',                            // 7.1
            28
        ],
        // 李四数据
        [
            '李四', 'YY公司制服定制项目', '执行', 800, '2024-11-05',
            '递进', '执行',
            '100%', '100%', '100%', '100%',  // 1.1-1.4
            '100%', '100%', '100%',          // 2.1-2.3
            '100%', '100%', '100%',          // 3.1-3.3
            '100%', '100%', '100%',          // 4.1-4.3
            '0%',                            // 5.1
            '0%',                            // 6.1
            '0%',                            // 7.1
            26
        ],
        // 王五数据
        [
            '王五', 'ZZ企业工装批量采购', '签约', 300, '2024-12-05',
            '接触', '签约',
            '100%', '100%', '100%', '100%',  // 1.1-1.4
            '100%', '100%', '100%',          // 2.1-2.3
            '100%', '100%', '100%',          // 3.1-3.3
            '100%', '100%', '100%',          // 4.1-4.3
            '100%',                          // 5.1
            '100%',                          // 6.1
            '100%',                          // 7.1
            30
        ]
    ];

    // 创建工作簿
    const wb = XLSX.utils.book_new();
    const ws = XLSX.utils.aoa_to_sheet(testData);

    // 设置列宽
    const colWidths = [
        { wch: 12 }, { wch: 22 }, { wch: 10 }, { wch: 14 }, { wch: 18 },
        { wch: 12 }, { wch: 12 }
    ];
    for (let i = 0; i < 16; i++) {
        colWidths.push({ wch: 28 });
    }
    colWidths.push({ wch: 14 });
    ws['!cols'] = colWidths;

    XLSX.utils.book_append_sheet(wb, ws, '销售数据');

    // 写入文件
    XLSX.writeFile(wb, '测试数据.xlsx');

    console.log('测试数据文件已生成：测试数据.xlsx');
    console.log('');
    console.log('包含3名销售员：');
    console.log('1. 张三 - 项目金额500万，从接触推进到深入');
    console.log('2. 李四 - 项目金额800万，从递进推进到执行');
    console.log('3. 王五 - 项目金额300万，从接触推进到签约（全部完成）');

} else {
    console.log('此脚本需要在Node.js环境中运行');
    console.log('或者直接在浏览器中点击"加载测试数据"按钮');
}
